const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const h=fs.readFileSync(path.resolve(__dirname,'../server-dropbox-helper.cjs'),'utf8');test('unknown files are not rejected by extension allowlist',()=>{assert.equal(h.includes('MEDIA_EXTENSIONS.has(ext) &&'),false);assert.ok(h.includes('safeUploadFilename(raw)'));});